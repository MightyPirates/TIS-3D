pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

apply from: 'minecraft.gradle'
rootProject.name = "tis3d-${minecraft_version}-${minecraft_sdk}"
